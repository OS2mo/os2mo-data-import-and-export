version: '3.4'

services:
  os2modataimportandexport:
    image: os2modataimportandexport
    build:
      context: .
      dockerfile: Dockerfile
    command: ["sh", "-c", "pip install debugpy -t /tmp && python /tmp/debugpy --wait-for-client --listen 0.0.0.0:5678 integrations/SD_Lon/sd.py "]
    ports:
      - 5678:5678
