To run a full SD import:

1) Run the importer with impor of all history enabled
2) Perform a fix-organisation to import all known future departments
3) Perform an initial ChangedAt run to initialise ChangedAtRuns-db and to import all currently known future changes
4) Run the daily ChangedAt.
